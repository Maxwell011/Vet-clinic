/* Database schema to keep the structure of entire database. */

CREATE TABLE animals (
    name varchar,
    date_of_birth date,
    id integer,
    escape_attempts integer,
    neutered boolean,
    weight_kg float
);
